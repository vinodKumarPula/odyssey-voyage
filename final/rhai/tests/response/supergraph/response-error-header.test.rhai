//TODO: Add tests for response-error-header.rhai. Since the rhai-test framework does not support response.headers as
// it was not built in apollo_mocks::get_supergraph_service_response()

//TODO: Either raise issue with rhai-test framework to support response.headers, response.body or use a different approach to test this.